# tests/expected_commands.sh - expected launch commands per recipe (SCAFFOLD)
#
# TODO: populate with the expected `vllm serve ...` command for each recipe,
# keyed by recipe name, once run-recipe.py dry-run output is finalized.
